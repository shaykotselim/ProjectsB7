let shoppingCart = {
    book:1, 
    pen:3, 
    laptop:5,
    keybord: 7,
}

const keys = Object.keys(shoppingCart);

console.log(keys);
const keys2 = Object.values(shoppingCart);
console.log(keys2);
